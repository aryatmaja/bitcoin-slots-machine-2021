version: 2.1
jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Run tests
          command: echo 'replace me with real tests!' && false
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Build an artifact
          command: touch example.txt
      - store_artifacts:
          path: example.txt
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: deploy
          command: "#e.g. ./deploy.sh"
  evals-test-assertions-job:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - evals/test:
          assertions: assertions.json
          metrics: eval_results.json
          results: test_results.xml
  playwright-test:
    docker:
      - image: mcr.microsoft.com/playwright:v1.15.0-focal
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Playwright tests
          command: |
            # Ensure the correct Node.js version is used
            npx playwright test --reporter=dot
workflows:
  example:
    jobs:
      - test:
          context: []
      - build:
          requires:
            - test
          context: []
      - deploy:
          requires:
            - test
          context: []
  test-eval-workflow:
    jobs:
      - evals-test-assertions-job
  playwright-workflow:
    jobs:
      - playwright-test
orbs:
  evals: circleci/evals@2.0
